server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/hyot88/msa-order-claim
          searchPaths: config-server/config
          default-label: master

# http://localhost:8888/application/default